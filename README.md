# churn-prediction-ml-models
Machine learning analysis for customer churn prediction using Random Forest and Support Vector Machine (SVM). The project evaluates model performance using metrics such as AUC, recall and F1-score, and applies hyperparameter tuning with GridSearchCV.
